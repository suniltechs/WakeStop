# Use separate keys so the native Maps key can be restricted to the app's
# package/bundle identifier while the Places web-service key is restricted to
# only the Places API (New) and protected with a small quota.
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=
